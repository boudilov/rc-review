# RC REVIEW — прогресс и заметки для продолжения работы

Презентация о работе ресурсных центров (РЦ) UCampus. Живой сайт: `index.html` +
`pages/NN/index.html` (по одному фрагменту на страницу), стилистика — `styles.md`.
Репозиторий: https://github.com/boudilov/rc-review (публичный).

**Ветка `Alternative`** — полная переверстка презентации под новый план (83
страницы вместо исходных 40), см. «Новый план и нумерация» ниже. `master` —
старая версия (40 страниц), не трогается, служит источником для восстановления
старого контента (`git show master:pages/NN/index.html`).

## Как запустить

```bash
cd RC_Review
python -m http.server 8936
```
Открыть `http://localhost:8936`. Конфиг превью уже лежит в `.claude/launch.json`.

## Новый план и нумерация (ветка Alternative)

Пользователь прислал новый план презентации с 8 блоками; в исходном плане была
опечатка (номер `[3]` использован дважды — для интро и для начала блока РОЛЬ),
поэтому все номера блока РОЛЬ и дальше сдвинуты на **+1** относительно
исходного текста плана. Итоговая нумерация (83 страницы):

| Блок | Страницы |
|---|---|
| 1. ИНТРО | 1–3 |
| 2. РОЛЬ | 4–19 |
| 3. СТРУКТУРА | 20–23 |
| 4. ПРОГРЕСС И РАЗВИТИЕ | 24–54 |
| 5. Текущие и планируемые проекты | 55–58 |
| 6. UCAMP | 59–70 |
| 7. Наша БАЦ | 71–82 |
| 8. Приглашение на выставку | 83–84 |

**Важно:** количество страниц блока БАЦ увеличили на 1 (была 71–81, стала
71–82) по просьбе пользователя, поэтому `TOTAL` в `index.html` теперь **84**,
не 83. Всё, что раньше было страницами 82–83 (блок «Приглашение на
выставку»), сдвинуто на +1 → 83–84. При очередном изменении количества
страниц: обновить `TOTAL`, все `data-page`/`aria-label` в `<button
class="seg">`, все `<section class="page" data-page="…">`, переименовать
папки `pages/NN`, и поправить текст `Страница NN / M` во всех оставшихся
плейсхолдерах (`grep -rl page-placeholder pages`).

Заголовки блоков (страницы 4, 20, 24, 55, 59, 71, 82 и т.д.) — крупный
центрированный uppercase-текст без киккера (формат страницы 3), **без**
подписи номера блока — киккер с номером блока убрали по просьбе пользователя.

## Архитектура (важно помнить)

- **Один файл — один каркас, остальное — фрагменты.** `index.html` — тулбар
  + JS-загрузчик, который через `fetch` подтягивает `pages/NN/index.html` в
  контейнер активной страницы. Общее число страниц задано в JS как
  `var TOTAL = 83;`. Тулбар теперь сгруппирован по блокам плана — сегменты
  обёрнуты в `.seg-group[data-block="…"]` с более широким зазором между
  группами и подписью блока во всплывающей подсказке при наведении
  (например «66 · UCAMP»). Каждый сегмент имеет `data-block` — используется
  для тултипа. Сегменты уменьшены до 10px ширины, чтобы 83 штуки влезали.
  Placeholder-папки `pages/41`…`pages/83` созданы по шаблону старых
  плейсхолдеров (`<div class="page-placeholder">Страница NN / 83</div>`).
- **Фрагменты — это только HTML+CSS, БЕЗ рабочего JS.** `<script>` внутри
  фрагмента, вставленного через `innerHTML`, не выполняется браузером —
  это ограничение архитектуры, не баг. Интерактивные штуки на статических
  страницах делаются чистым CSS (`:target` для модалок, `:checked`/`:has()`
  для тумблеров, `:hover` для тултипов).
- **Тулбар**: у каждого сегмента теперь есть `data-title` (1–2 слова о
  содержании конкретной страницы, не блока) — подсказка при наведении
  показывает «NN · data-title», а не «NN · BLOCK» как раньше. Плюс
  постоянный индикатор `#pageNum` («76 / 83») рядом с иконкой «i»,
  обновляется в `setActive()`. При добавлении новой страницы — не забыть
  прописать `data-title` в её `<button class="seg">` в `index.html`.
- **Кэш — частый источник багов**, причём не только для фрагментов.
  `loadPage()` в `index.html` дописывает `?_=Date.now()` к fetch фрагмента и
  к `src` любых `<img>`/`<iframe>` внутри него. НО: вложенные `<script
  src="...">` внутри iframe-приложений (`pages/rc-app/history.html`,
  `pages/rc-app/structure.html`) этим механизмом не покрывались — нашли и
  починили: оба файла теперь грузят свой JS через
  `<script type="module">import("./X.js?_="+Date.now())</script>` вместо
  статического `<script src="X.js">`. Если правишь `pages/ucampus-viz/`
  или любое другое embedded-приложение и правки не видны — проверяй именно
  этот путь кэширования, не только fetch фрагмента.
- **id должны быть уникальны по всему документу** — все 83 фрагмента
  одновременно живут в DOM (скрыты `display:none`).
- **Курсор** везде переопределён на маленькую полупрозрачную точку
  (SVG data-URI в `<html,body>` в `index.html`).
- **`pages/rc-app/`** — встроенное веб-приложение (история РЦ + орг-
  структура), теперь на страницах **21** (история) и **22** (структура).
  Исходники `structure.xlsx`/`history.xlsx` + `convert.py` там же —
  `python convert.py` внутри `pages/rc-app/` пересобирает оба `.json` из
  xlsx. `history.xlsx` пользователь дополняет сам (в этой сессии добавились
  новые записи об открытии мастерских, 43 строки вместо прежних ~30).
  Размеры узлов/шрифтов в `viz.js` (`NODE_STYLE`) увеличены ~на 40% для
  читаемости на проекторе (`root: 40/15, tg: 34/14, workshop: 31/11,
  person: 20/10` — было `28/11, 24/10, 22/8, 14/7`).
- **`pages/ucampus-viz/`** — второе embedded-приложение, теперь используется
  на странице **2** (интро, «визуализация кампуса на время сбора гостей») —
  переиспользуется также на стр. 60 (блок UCAMP, ещё не собрана). Исходник
  обновлялся в этой сессии (файл вырос с ~50 КБ до ~70 КБ) — теперь пытается
  подключаться к живым событиям через прод-API (`ucampus.u.university`,
  Pusher/WebSocket) вместо локальной эмуляции; локально (не с прод-домена)
  показывает «НЕТ СВЯЗИ» — это ожидаемо, CORS.
- **История-таймлайн (стр. 21) — ручное управление, без автоплея.**
  `history-main.js`: убрали `setInterval`-автоплей, вернули ручной шаг —
  **Space** вперёд, **Ctrl+Space** назад (плюс уже существовавшие клик ЛКМ
  вперёд / ПКМ назад). Раскрытие идёт кумулятивно: каждый год-степ
  «открывает» все записи с `year <= revealYear`, не только текущий год.
- **Орг-структура (стр. 22) — свёрнута по умолчанию.** Разворачивается
  стрелкой **ArrowUp** (сворачивается **ArrowDown**) — см. `structure-
  main.js`, `viz.expandDiagram()`/`collapseDiagram()`. Это осознанное
  поведение виджета, не баг.

## Переиспользуемые CSS-паттерны

- `.accent-group` + `.accent-item` — при наведении на элемент внутри группы
  остальные тускнеют, у наведённого — `text-shadow`-свечение. Определено в
  `index.html`, работает на любой странице.
- **Блок-заголовки** (страницы 4, 20, 24…): центрированный uppercase-текст
  72px, без киккера — см. страницы 3/4 как образец.
- **Заголовок мастерской на статистических страницах**: `ws{N}-title`,
  28px bold, сверху страницы, перед сканом/графиками — единообразно на всех
  страницах блока ПРОГРЕСС И РАЗВИТИЕ.
- **Плейсхолдер «скан мастерской»**: `border: 1px dashed rgba(255,255,255,
  0.18)`, текст по центру `«Скан мастерской «X» · ожидает фото»` — стоит
  вместо реального фото на всех страницах мастерских (фото ещё не собраны).
- **Плейсхолдер «ожидает данных»**: для цифр — тусклое «—» (`rgba(255,255,
  255,0.18)`, тот же размер шрифта, что у реальных чисел) + подпись
  `rgba(230,230,240,0.45)` с суффиксом «· ожидает данных»/«· уточняется».
  Для кейсов/текстовых блоков — kicker+title как обычно, тело — та же
  подпись-плейсхолдер.
- **Графики SELF/CLASS год-к-году** (`wsChart-*`, страницы 27–51 чётные):
  чёрный фон, две сглаженные (Catmull-Rom) линии без заливки — серая
  `rgba(255,255,255,0.35)` для 2024/25, белая `rgba(255,255,255,0.92)` для
  2025/26, легенда сверху, тонкая горизонталь нуля внизу графика
  (`rgba(255,255,255,0.12)`, добавлена по просьбе — раньше без неё
  Catmull-Rom мог "перелетать" за пределы viewBox у резких провалов в 0 и
  визуально выглядел как разрыв линии — контрольные точки безье теперь
  клэмпятся в `[0, height]`, см. `catmull_rom_to_bezier(points, y_min, y_max)`
  в генераторе). Число за 2025/26 — крупное белое, под ним мельче серым
  число за 2024/25, перед белым числом — цветной дельта-бейдж (**зелёная**
  ▲ / **красная** ▼ + %, «новое» если предыдущий год был 0).
- **Ранжированные bar-списки** (топ-5 программ, направления, школы,
  юрлица): `label + track + fill + value`, alpha/ширина трека кодирует
  величину. Для «доля от общего» (стр. 45) — ширина = % от суммы колонки
  (не от максимума), значение — тоже %; для сравнения год-к-году без
  цифр — вторая тонкая (2px) полоса под основной, без подписи
  (`visibility:hidden`, не `display:none` — иначе не совпадает выравнивание
  с полосой сверху).
- **Heatmap-таблицы** (стр. 25 CLASS/SELF, стр. 53 IDLE): alpha плитки =
  `0.08 + 0.82 * (v / vmax)` где `vmax` — максимум по ОБОИМ годам в группе
  колонок (для % — `vmax = 100`, не максимум датасета). Текст плитки
  становится тёмным (`#0a0a0a`) когда alpha > ~0.55. Дельта — зелёная ▲ /
  красная ▼ п.п. или %, «новое» для нулевой базы. Для IDLE стрелка отражает
  фактическое изменение числа, а не оценку "хорошо/плохо" — рост простоя
  = красный, снижение = зелёный (можно перепутать местами, если копируешь
  паттерн — идея в том что «простой растёт» это плохо, а не то что «▲
  всегда красный»).
- Плавные линии — Catmull-Rom → кубические Bezier, генерируются офлайн в
  Python, путь `d` вставляется как статика в SVG.

## Что сделано по страницам (ветка Alternative, новая нумерация)

| # | Блок | Содержимое |
|---|---|---|
| 1 | ИНТРО | Лого RC в космосе, звёздный canvas-фон (без изменений от старой стр. 1) |
| 2 | ИНТРО | Визуализация кампуса — iframe `pages/ucampus-viz/` (переиспользован со старой стр. 31) |
| 3 | ИНТРО | Титул «RC Review Day», киккер «Ресурсные центры · UCampus» |
| 4 | РОЛЬ | Заголовок блока «Роль ресурсных центров в UU» |
| 5 | РОЛЬ | «Ресурсные центры — это инфраструктура…» (текст плана) |
| 6 | РОЛЬ | «От теории к навыку» + абзац |
| 7 | РОЛЬ | Цифра 39 482 ч (практика 25/26, пересчитано и сверено с исходной цифрой) — сравнение с часами теории ждёт данных |
| 8 | РОЛЬ | «Индустриальный уровень внутри кампуса» + список оборудования (просто текст, без таблицы) |
| 9 | РОЛЬ | Цифра — 1 145 единиц техники в прокате |
| 10 | РОЛЬ | «Проекты студентов с минимальными расходами» + абзац |
| 11 | РОЛЬ | 2 кейса: «Фешн-съёмка на плёночную камеру» (28 000 ₽) и «Архитектурный макет» (вилка 61 500–103 500 ₽, мин/макс по каждой строке) — компактная вёрстка, оба на одной странице |
| 12 | РОЛЬ | «Площадка, где университет становится видимым» + абзац |
| 13 | РОЛЬ | Цифра — 50+ мероприятий проведено с участием РЦ |
| 14 | РОЛЬ | «Аргумент при выборе университета» + абзац |
| 15 | РОЛЬ | Топ-10 конкурентов по числу мастерских (из «Топ-10 конкурентов» вкладки анализа конкурентов, файл обновлён до FINAL5000) + список «Особенности мастерских UU» внизу. Клик по любой строке топ-10 открывает CSS `:target`-модалку с полной матрицей покрытия (UU + 10 конкурентов × 23 типа мастерских, только галки, без описаний) |
| 16 | РОЛЬ | «Собственный ресурс для нужд РЦ» + абзац |
| 17 | РОЛЬ | Цифра — км печати на ДОДы 25/26, ждёт данных |
| 18 | РОЛЬ | «Инфраструктура, которая может зарабатывать» + абзац |
| 19 | РОЛЬ | Кейс «Бартер по мебели» + потенциальный кейс «Коворкинг» — детали ждут данных |
| 20 | СТРУКТУРА | Заголовок блока «Структура» |
| 21 | СТРУКТУРА | Таймлайн истории РЦ — iframe `pages/rc-app/history.html`, ручное управление (см. архитектуру) |
| 22 | СТРУКТУРА | Орг-структура — iframe `pages/rc-app/structure.html`, крупнее узлы, сворачивается/разворачивается стрелками |
| 23 | СТРУКТУРА | Крупные цифры: 36 сотрудников · высшее образование (уточняется) · 12 работают 2+ года · 9 работают 5+ лет · 1 876 м² (17% от всех площадей) |
| 24 | ПРОГРЕСС | Заголовок блока «Прогресс и развитие» |
| 25 | ПРОГРЕСС | Heatmap CLASS/SELF по 10 мастерским (без библиотеки и проката), 24/25→25/26, % прироста — порядок мастерских как в новом плане страниц |
| 26 | ПРОГРЕСС | Центр информационных ресурсов (Библиотека) — шаблон: скан + 2 плейсхолдера (выдача книг, посещения), данных пока нет |
| 27–42 | ПРОГРЕСС | 8 мастерских по 2 страницы (скан+график год/год, затем топ-5 программ+направления): Фешн, Текстиль, Ручная печать, 3D, Ювелирная, Керамика, Фотостудия Артплей, Фотостудия Фабрика |
| 43 | ПРОГРЕСС | Прокат: скан + 708 → 1 145 единиц техники (24/25 → 25/26), горизонтальная стрелка |
| 44 | ПРОГРЕСС | Прокат: «отдельный формат брони», 285 443 → 347 799 (+22%), помесячный тренд — восстановлено из старой версии (стр. 18) |
| 45 | ПРОГРЕСС | Прокат: кто использует технику — юрлица → школы → направления, % от общего объёма, тонкая полоса сравнения с 24/25 |
| 46 | ПРОГРЕСС | Прокат: топ-5 программ по использованию техники — восстановлено из старой версии (стр. 20), цифры пересчитаны и совпали день-в-день |
| 47–48 | ПРОГРЕСС | Принт-офис: скан+график, топ-5+направления (стандартный шаблон) |
| 49 | ПРОГРЕСС | Принт-офис: «Объёмы печати» 25/26 — 1 388 399 м² для студентов, 1 306 000 м² для школьных мероприятий |
| 50–51 | ПРОГРЕСС | Тёмная комната: скан+график, топ-5+направления (стандартный шаблон) |
| 52 | ПРОГРЕСС | Галерея: скан + «что было сделано» (перепланировка, ремонт, переезд на UCampus) |
| 53 | ПРОГРЕСС | IDLE: ранжированная таблица простоя по 10 мастерским — восстановлено из старой версии (стр. 21), цифры совпали |
| 54 | ПРОГРЕСС | «Общие выводы по мастерским» — шаблон, 4 пункта-плейсхолдера ждут содержания |
| 55 | ПРОЕКТЫ | Заголовок блока «Текущие и планируемые проекты» |
| 56 | ПРОЕКТЫ | «Коворкинг» — заголовок + плейсхолдер «ожидает данных» |
| 57 | ПРОЕКТЫ | «Личный баланс часов студента» — заголовок + плейсхолдер «ожидает данных» |
| 58 | ПРОЕКТЫ | «Система уровней пользователей» — заголовок + плейсхолдер «ожидает данных» |
| 59 | UCAMPUS | Заголовок блока «UCampus» |
| 60 | UCAMPUS | Визуализация кампуса — тот же iframe `pages/ucampus-viz/`, что на стр. 2 |
| 61 | UCAMPUS | «Что такое UCampus и для чего он нужен» — hero-статement (формат стр. 5) |
| 62 | UCAMPUS | «Операционный контур РЦ» + абзац (бронирование/доступ/коммуникация/учёт/аналитика/риски) |
| 63–68 | UCAMPUS | 6 функций по шаблону «заголовок + плейсхолдер скриншота»: Бронирование, Доступ, Коммуникация, Учёт оборудования, Аналитика, Контроль рисков — скриншоты ждут пользователя |
| 69 | UCAMPUS | Посещения платформы за год: 128 621 → 137 037 (+7%), помесячный график (формат стр. 44) — данные из `UCampus_Traffic-*.xlsx` |
| 70 | UCAMPUS | «Прогресс и развитие» (для UCampus) + подзаголовок, 4 пункта-плейсхолдера |
| 71 | БАЦ | Заголовок блока «БАЦ» |
| 72 | БАЦ | «РЦ as is» + абзац про раскиданность РЦ по корпусам без айдентики |
| 73 | БАЦ | «РЦ to be» + абзац про идею представить мастерские как систему (периодическая таблица элементов) |
| 74 | БАЦ | Демо-плитка «периодического элемента» (на примере Фотостудии, Ph) с легендой — что означает каждая надпись |
| 75 | БАЦ | Текущее состояние: все 21 РЦ-«элемент» тремя группами (Существующие/Независимые/Неоткрытые), с визуальным разделением на технологические подгруппы внутри «Существующих» |
| 76 | БАЦ | Объединение в «три хаба» (Tech Hub / Touch Hub / Think Hub) — та же плитка, перегруппированная в 3 ряда с подписью слева |
| 77 | БАЦ | «Цели 2032» — 3 цели списком (бренд, рост числа мастерских, выход за пределы Universal University) |
| 78 | БАЦ | «22 элемента под единым брендом» + Trust&Reputation / Dreamteam / Profit крупно, внизу — строка из всех 22 кликабельных плиток-элементов (модалка увеличения, тот же CSS `:target`-паттерн) |
| 79–82 | БАЦ | 4 визуализации «кампуса мечты» (University Creative Workshops Center), на всю страницу, `object-fit: contain` — источник картинок ниже |
| 83 | ВЫСТАВКА | «Описание выставки» — заглушка «ожидает данных» |
| 84 | ВЫСТАВКА | «QR-код выставки» — плейсхолдер-рамка, ждёт реального QR |

### Стандартный шаблон мастерской (страницы 27–51, кроме особых)

Генерируется Python-скриптом `gen_workshop_pages.py` (лежит в scratchpad
сессии, **не в репозитории** — см. «На заметку на будущее»). Две страницы на
мастерскую:
1. **Страница A** — заголовок + плейсхолдер скана + два графика год-к-году
   (SELF, CLASS) с легендой и дельта-бейджами.
2. **Страница B** — заголовок + 4 квадранта: топ-5 программ SELF/CLASS,
   направления SELF/CLASS (ранжированные bar-списки).

Данные считаются из `Stats 2026/*.xlsx` (pandas groupby по `Период`/
`Программа`/`Направление`), учебный год = авг–июль. Функция для повторного
использования: `chart_svg()`, `bar_list()`, `page_a()`, `page_b()` в
generator-скрипте.

### Блок БАЦ — «периодическая система» РЦ (стр. 71–81)

Концепция: мастерские/РЦ представлены как «химические элементы» на плитках
в стиле периодической таблицы Менделеева. Каждая плитка (`el74-tile`/
`el75-tile`/`el76-tile` — стили дублируются под номером страницы, см. паттерн
ниже) — квадрат 340px (демо на стр. 74) или 104px (сводные таблицы стр.
75–76) с 4 подписями по углам + большой символ в центре:
- **слева сверху** — номер аудитории («корпус / кабинет», например
  «2 / 4.06»); для ещё не открытых РЦ — плейсхолдер «X / X.XX».
- **справа сверху** — порядковый номер РЦ в общей системе (1–22).
- **по центру** — символ (2 буквы, кириллица недопустима — используются
  латинские сокращения по типу химических символов, иногда специально
  обыгрывающие реальный элемент: Галерея → **Ga** как настоящий Галлий).
- **слева снизу** — год основания РЦ (из `history.json`/`history.xlsx`,
  для неоткрытых — плановый год).
- **справа снизу** — полное название РЦ (мелким капсом).

Стр. 74 — объяснение системы обозначений на примере одной плитки
(Фотостудия/Ph). Стр. 75 — все 21+1 «элементов» тремя статусными группами:
**Существующие** (12, сплошная заливка плитки) / **Независимые** (3) /
**Неоткрытые** (6) (две последние — `.ghost`, пунктирная рамка, приглушённые
цвета — переиспользуй паттерн для «ещё не реализовано»); внутри
«Существующих» увеличенные горизонтальные отступы (`margin-right: 26px`)
между Rn/3D и Cr/Fs показывают деление на технологические подгруппы. Стр.
76 — те же плитки перегруппированы в концепцию «трёх хабов»: **Tech Hub**
(Ph, Pr, Fp, Rn, Au, Vr) / **Touch Hub** (3D, Jw, Cr, Fs, Tx, Pm, Dr, Gl, Fd)
/ **Think Hub** (Ga, Ch, Al, Uc, Lb, Ai), с подписью хаба слева от ряда.

Плитки на стр. 75–76 **кликабельны** — открывают увеличенную версию (340px,
формат стр. 74) поверх затемнённого фона. Реализовано чистым CSS `:target`
(без JS, т.к. фрагменты его не выполняют — см. «Архитектура»): каждая
маленькая плитка — `<a href="#el{N}-m-{Symbol}">`, где-то в конце того же
фрагмента лежит `<div id="el{N}-m-{Symbol}" class="el{N}-modal">` с
`display:none` по умолчанию и `display:flex` при `:target`; закрытие —
клик по фону (`<a href="#">`, во весь экран, под плиткой в DOM-порядке) или
по кнопке «Закрыть ✕». `id` уникальны за счёт префикса страницы (`el75-m-`,
`el76-m-`), т.к. один и тот же символ (например Ph) встречается на разных
страницах. Генерируется скриптом (`gen_page75.py`/`gen_page76.py` в
scratchpad, не в репозитории) — при добавлении новой сводной таблицы РЦ
проще писать похожий генератор, чем руками копировать 20+ модалок.

Стр. 79–82 — 4 архитектурные визуализации-мудборды «University Creative
Workshops Center» (концепт кампуса мечты, чёрно-белый скетч-стиль), взяты
из той же папки, что и `ELMNTS.xlsx` — **`C:\DATA\UU\UCampus\БАЦ\image_2026-
09-15_*.png`** (4 файла, ~3.2 МБ каждый). Скопированы **внутрь репозитория**
как `pages/{79,80,81,82}/campus.png` (по образцу `Stats 2026/` — открытое
решение хранить сырые ассеты в паблик-репо, см. ниже про размер). Каждая
страница — просто `<img src="pages/NN/campus.png" style="object-fit:
contain">` на весь фрагмент, без обёртки.

**На заметку**: 4 PNG ~13 МБ суммарно легли в публичный репозиторий без
отдельного подтверждения — если позже понадобится сжать/убрать, картинки
сейчас только в `pages/79-82/campus.png`, исходники остаются в `БАЦ/`
вне репо.

Источник данных о плитках — **`C:\DATA\UU\UCampus\БАЦ\ELMNTS.xlsx`**
(вне репозитория, пользователь ведёт сам). Колонки: `символ`, `надпись
лево верх` (ауд.), `надпись право верх` (номер), `надпись лево низ` (год),
`надпись право низ` (название). Открывать через `openpyxl` с
`data_only=True`; если лист выглядит пустым — файл мог быть открыт в Excel
и не сохранён (рядом лежит lock-файл `~$ELMNTS.xlsx`), попроси пользователя
сохранить и перечитай. Годы основания для «Существующих» сверены с
`pages/rc-app/history.json`.

### Блок UCampus (стр. 59–70)

- Посещения (стр. 69) — источник `Stats 2026/UCampus_Traffic-2024-08-01-
  2025-07-31.xlsx` и `…-2025-08-01-2026-07-31.xlsx` (экспорт аналитики,
  лист `Report`, данные с 5-й строки, колонки `Period`/`Sessions`,
  дневная гранулярность — агрегировалась по месяцам такой же
  Catmull-Rom-функцией, что и графики мастерских).
- Стр. 63–68 (Бронирование/Доступ/Коммуникация/Учёт оборудования/
  Аналитика/Контроль рисков) — заголовок + дэшед-плейсхолдер под
  скриншот интерфейса UCampus, скриншотов пока нет.

## Источники данных

- `Stats 2026/*.xlsx` — по каждой мастерской (01–10) файлы `CLASS`/`SELF`/
  `IDLE`; по прокату (`11_Прокат_*`) — `BOOKINGS`/`IDLE`/`RESOURCES`/`SELF`.
  Копия лежит **внутри репозитория** (публично!) — осознанное решение
  пользователя.
- Учебный год = 1 авг — 31 июля. «Прошлый»/24-25 = 2024-08…2025-07,
  «этот»/25-26 = 2025-08…2026-07.
- CLASS/SELF файлы мастерских: колонки `Юр.лицо`, `Школа`, `Направление`,
  `Программа`, `Период` (YYYY-MM), `Часы`. Один ряд = агрегат часов, НЕ
  отдельная бронь.
- Прокат `RESOURCES`: `Месяц`, `Ресурс`, `Школа`, `Программа`, `Кол-во`,
  `Юрлицо` — нет `Направление`, поэтому разрез по направлениям для проката
  берётся из `SELF`-файла проката (часы, а не «Кол-во» — подписано явно).
  `BOOKINGS`: `Месяц`, `Кол-во брони`, `Кол-во отработано`, `Кол-во
  позиций`, `Часов отработано` — использовалась для проверки объёма
  использования (285 443 → 347 799 на стр. 44, те же данные что дал
  RESOURCES).
- `IDLE`-файлы: `Интервал`, `Доступное время (ч)`, `Занятое время (ч)`,
  `Свободное время (ч)`, `Процент занятости`, `Процент простоя`. Простой% =
  `сумма(Свободное)/сумма(Доступное)` за год (часо-взвешенно), НЕ среднее
  готовых месячных процентов. Использовано на стр. 25 (часы) и 53 (%).
- Анализ конкурентов: `C:\DATA\UU\UCampus\Анализ мастерских конкурентов\
  FINAL5000_UU_konkurenty_matritsa_masterskih.xlsx` (сменился с FINAL3000 —
  файл вне репозитория, пользователь обновляет сам; если снова увидишь
  FINAL3000/пустой лист «Топ-N» — спроси про новую версию файла), вкладка
  «Топ-10 конкурентов» — используется на стр. 15 (10 конкурентов + UU-эталон,
  с учётом мест-делёжек №2–3/4–5/6–7/8–9). **Не пытайся досчитывать топ
  самостоятельно** по сырым вкладкам «Матрица — Москва/СПб» — там много строк
  с `н/д` (не исследовано) вместо прочерка, наивный подсчёт «есть хоть что-то»
  зачисляет их как заполненные и выдаёт мусорных лидеров (видел это на
  FINAL3000 — характерный пример «АЗЫ» с 23/24 при полностью пустом «н/д»-ряде).
  Топ-N-вкладка — результат ручной курации (дедуп школ вроде ВШЭ, разбитых на
  8 строк, исключение неисследованных), доверять только ей. Открывать через
  `openpyxl`, имена вкладок и данные — кириллица, при работе в Git Bash/
  PowerShell консоль может показывать мусор из-за кодировки, но сами данные
  целые (писать/читать файлы всегда с explicit `encoding="utf-8"`, не
  полагаться на
  системную кодировку по умолчанию на Windows).
- `Stats 2026/Originals/` — те же данные с англ. именами файлов, для сверки;
  не менять, не использовать напрямую.

## Ждёт данных / пользовательского ввода

- **Фото-сканы мастерских** (10 шт, включая библиотеку) — везде сейчас
  плейсхолдер.
- **Стр. 7**: часы теории для сравнения с 39 482 ч практики.
- **Стр. 17**: км печати на ДОДы (стр. 9 и 13 уже закрыты — 1 145 техники и
  50+ мероприятий соответственно).
- **Стр. 11**: возможно ещё 1 кейс (сейчас 2 из «2–3» по плану).
- **Стр. 15**: реальные цифры сравнения с конкурентами (сейчас только топ-5
  по числу мастерских + качественный список УТП).
- **Стр. 19**: детали кейса бартера по мебели, потенциальный кейс коворкинга.
- **Стр. 23**: % сотрудников с высшим образованием.
- **Стр. 26**: источник данных по библиотеке (выдача книг, посещения) — нет
  в Stats 2026, нужен отдельный файл/API.
- **Стр. 44** (частично не запрашивалось, но потенциально): рост парка
  техники по годам не запрашивался пользователем в новом виде — на стр. 43
  цифры 708→1145 даны пользователем напрямую (не из RESOURCES — там нет
  истории закупок, только использования).
- **Стр. 54**: сами выводы по блоку — текст.
- **Стр. 56–58** (проекты «Коворкинг», «Личный баланс часов студента»,
  «Система уровней пользователей»): описания проектов — пока только
  заголовки.
- **Стр. 63–68**: скриншоты интерфейса UCampus (Бронирование, Доступ,
  Коммуникация, Учёт оборудования, Аналитика, Контроль рисков).
- **Стр. 70**: сами пункты «что сделано / что планируется» по UCampus —
  пока 4 плейсхолдера.
- **Стр. 74–76**: год основания и номер аудитории для «Независимых» и
  «Неоткрытых» РЦ — в `ELMNTS.xlsx` сейчас плейсхолдер `X / X.XX`.
- **Стр. 83**: текст описания выставки.
- **Стр. 84**: реальный QR-код (ссылка/данные ещё не даны).

## На заметку на будущее

- **Python-скрипты для чисел/графиков** пишутся как одноразовые файлы в
  scratchpad сессии, в репозиторий не попадают. Самый developed —
  `gen_workshop_pages.py` (шаблон мастерской, см. выше) — если он понадобится
  в новой сессии, его придётся написать заново по описанным здесь паттернам
  (pandas groupby, Catmull-Rom с клэмпингом, delta-бейджи зелёный/красный).
- Каждый раз при открытии `Stats 2026/*.xlsx` через pandas переименовывай
  колонки сразу после чтения (`df.columns = [...]`) — сырые имена кириллицей
  плохо выживают в терминале/echo, хотя в самих данных всё нормально при
  чтении/записи с explicit UTF-8.
- Прежде чем коммитить новые сырые данные — если они большие/чувствительные,
  сначала спросить пользователя.
- Названия мастерских (обновлены в этой сессии): **Фешн** (была «Одежда»),
  Текстиль, Ручная печать, 3D, Ювелирная, Керамика, **Фотостудия Артплей**
  (была «Артплей»), **Фотостудия Фабрика** (была «Фабрика»), Тёмная
  комната, Принт-офис (коды 01–10 в этом порядке в Stats 2026; 11 = Прокат).
- Если долго не видишь правки в embedded-приложении (`rc-app`,
  `ucampus-viz`) — почти всегда кэш вложенного `<script src>`, см.
  «Архитектура» выше про фикс с `import()`.
